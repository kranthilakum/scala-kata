// Find the number of elements of a list
def numberOfElements[A](elements: List[A]): Any = elements.length

numberOfElements(List(1, 2, 3, 4, 5))
numberOfElements(List("John", "Jack", "Jose"))
numberOfElements(List())
